const ProductosManager=(()=>{let items=[];let edit=null;async function cargar(){items=await ApiClient.get('productos.php');document.getElementById('productos-lista').innerHTML=items.map(p=>`<div class="list-row"><strong>${p.nombre}</strong><span>${p.categoria}</span><span class="text-muted">${p.material_promocional||'—'}</span><button class="btn btn-sm btn-ghost" onclick="ProductosManager.abrirModal(${p.id_producto})">Editar</button><button class="btn btn-sm btn-danger" onclick="ProductosManager.eliminar(${p.id_producto})">Borrar</button></div>`).join('')||'<div class="empty-state">No hay productos.</div>';}function abrirModal(id=null){edit=id;document.getElementById('form-producto').reset();document.getElementById('modal-producto-titulo').textContent=id?'Editar Producto':'Nuevo Producto';if(id){let p=items.find(x=>x.id_producto==id);document.getElementById('prod-nombre').value=p.nombre;document.getElementById('prod-categoria').value=p.categoria;document.getElementById('prod-material').value=p.material_promocional||'';}document.getElementById('modal-producto').classList.add('open');}function cerrarModal(){document.getElementById('modal-producto').classList.remove('open');edit=null;}async function guardar(e){e.preventDefault();let d={nombre:document.getElementById('prod-nombre').value.trim(),categoria:document.getElementById('prod-categoria').value.trim(),material_promocional:document.getElementById('prod-material').value.trim()};if(edit){d.id_producto=edit;await ApiClient.put('productos.php',d);}else await ApiClient.post('productos.php',d);cerrarModal();cargar();showToast('Producto guardado ✓');}async function eliminar(id){if(confirm('¿Borrar este producto?')){await ApiClient.delete('productos.php?id='+id);cargar();}}document.addEventListener('DOMContentLoaded',()=>{document.getElementById('form-producto')?.addEventListener('submit',guardar);document.querySelector('[data-tab="productos"]')?.addEventListener('click',cargar);});return{cargar,abrirModal,cerrarModal,eliminar};})();
function obtenerUbicacion(){if(!navigator.geolocation){showToast('Tu navegador no soporta geolocalización','error');return;}showToast('Solicitando ubicación…','info');navigator.geolocation.getCurrentPosition(p=>{document.getElementById('med-lat').value=p.coords.latitude.toFixed(7);document.getElementById('med-lng').value=p.coords.longitude.toFixed(7);showToast('Ubicación obtenida ✓');},e=>showToast('No se pudo obtener la ubicación: '+e.message,'error'),{enableHighAccuracy:true,timeout:10000});}
